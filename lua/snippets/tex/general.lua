return {
  s({ trig = ';ie', condition = in_mathzone, snippetType = 'autosnippet' }, fmta('i.e.', {})),
  s({ trig = ';eg', condition = in_mathzone, snippetType = 'autosnippet' }, fmta('e.g.', {})),
}
